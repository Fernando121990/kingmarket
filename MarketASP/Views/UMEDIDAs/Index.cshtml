@model IEnumerable<MarketASP.Models.UMEDIDA>

@{
    ViewBag.Title = "U.Medida";
    ViewBag.pTitle = "U.Medida";
    ViewBag.pageTitle = "Mantenimiento";
}

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <div>
                    <a href="@Url.Action("Create")" class="btn btn-success mb-2"><i class="mdi mdi-plus me-2"></i> Agregar U.Medida</a>
                </div>
                <div class="table-responsive mt-3">
                    <table id="datatable" class="table table-centered datatable dt-responsive nowrap " style="border-collapse: collapse; border-spacing: 0; width: 100%;">
                        <thead class="thead-light">
                            <tr>
                                <th>
                                    Descripción
                                </th>
                                <th>
                                    Cod.Sunat
                                </th>
                                <th>
                                    Desc.Sunat
                                </th>
                                <th>
                                    Activo
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.sdesc_umed)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.scodsunat_umed)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.ssunat_umed)
                                </td>
                                <td>
                                    <input type="checkbox" checked="@item.nesta_umed" readonly />
                                </td>
                                <td id="tooltip-container3">
                                    <a href="@Url.Action("Edit", new { id = item.ncode_umed })" class="me-3 text-primary" data-bs-container="#tooltip-container3" data-bs-toggle="tooltip" data-bs-placement="top" title="Editar"><i class="mdi mdi-pencil font-size-18"></i></a>
                                    <a href="@Url.Action("DeleteUmed", new { id = item.ncode_umed })" class="text-danger" data-bs-container="#tooltip-container3" data-bs-toggle="tooltip" data-bs-placement="top" title="Eliminar"><i class="mdi mdi-trash-can font-size-18"></i></a>
                                </td>
                            </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
    <!-- end row -->
