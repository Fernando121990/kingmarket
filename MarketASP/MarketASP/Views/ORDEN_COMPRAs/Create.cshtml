@model MarketASP.Models.ORDEN_COMPRAS

@{
    ViewBag.Title = "Agregar Orden Compra";
    ViewBag.pTitle = "Agregar Orden Compra";
    ViewBag.pageTitle = "Orden Compra";
}
<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <input id="cnfdeci" name="cnfdeci" type="hidden" value="@ViewBag.deci" />
                    <input id="cnfigv" name="cnfigv" type="hidden" value="@ViewBag.igv" />
                    <input id="cnficbper" name="cnficbper" type="hidden" value="@ViewBag.icbper" />

                    @Html.Partial("_OrdenCompra")
                }
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/cliente")
    @Scripts.Render("~/bundles/ordencompra")
<script type="text/javascript">
        var urlGetProveedor = '@Url.Action("getProveedor", "Funciones",new { area = ""})';


        var urlGetDocuNumero = '@Url.Action("getDocuNumero", "Funciones",new { area = ""})';
        var urlGetDiasFormaPago = '@Url.Action("getDiasFormaPago", "Funciones",new { area = ""})';
        var urlGetUbigeo = '@Url.Action("getUbigeo", "Funciones")';
        var urlArticulos = '@Url.Action("getArticulos", "Funciones",new { area = ""})';
        var urlKardex = '@Url.Action("getKardex", "Funciones",new { area = ""})';
        var urlOCompraPrecio = '@Url.Action("getOcompraPrecio", "Funciones",new { area = ""})';
        var urlEditar = '@Url.Action("fnnumeroeditar", "Funciones", new { area = "" })';
        var urlorcoCrea = '@Url.Action("Create", "orden_compras")';
        var urlorcoLista = '@Url.Action("index", "orden_compras")';
</script>

}
