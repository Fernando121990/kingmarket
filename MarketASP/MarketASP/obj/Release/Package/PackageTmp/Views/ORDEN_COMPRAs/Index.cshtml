@model IEnumerable<MarketASP.Models.Pr_OCompraConsulta_Result>

@{
    ViewBag.Title = "Consultar";
    ViewBag.pTitle = "Consultar";
    ViewBag.pageTitle = "Orden de Compra";
}
<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <div>
                    <a href="@Url.Action("Create")" class="btn btn-success mb-2"><i class="mdi mdi-plus me-2"></i> Agregar Orden Compra</a>
                </div>
                <div class="table-responsive mt-3">
                    <table id="datatable" class="table table-centered datatable dt-responsive nowrap" data-bs-page-length="5" style="border-collapse: collapse; border-spacing: 0; width: 100%;font-size:12px">
                        <thead class="thead-light">
                            <tr>
                                <th>
                                    Proveedor
                                </th>
                                <th>T</th>
                                <th>
                                    No Doc.
                                </th>
                                <th>
                                    F.Compra
                                </th>
                                <th>
                                    Total
                                </th>
                                <th>Almacen</th>
                                <th>Situacion</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Proveedor)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.stipo_orco)
                                    </td>
                                    <td>
                                        @Html.ActionLink(item.documento, "Details", new { id = item.ncode_orco })
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.fecha)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TOTAL)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.almacen)
                                    </td>
                                    @*<td>
                                        @if (item.besta_orco == false)
                                        {
                                            <div class="badge badge-soft-warning font-size-12">No</div>
                                        }
                                        else
                                        {
                                            <div class="badge badge-soft-success font-size-12">Si</div>
                                        }
                                    </td>*@
                                    <td>
                                        @if (item.bcompra_orco == 0)
                                        {
                                            <div class="badge badge-soft-warning font-size-12">Pendiente</div>
                                        }
                                        else if (item.bcompra_orco == 1)
                                        {
                                            <div class="badge badge-soft-primary font-size-12">Parcial</div>
                                        }
                                        else
                                        {
                                            <div class="badge badge-soft-success font-size-12">Total</div>
                                        }
                                    </td>
                                    <td>
                                        @if (item.besta_orco == true && item.bcompra_orco == 0)
                                        {
                                            <a href="@Url.Action("Edit", new { id = item.ncode_orco })" class="btn btn-success btn-circle btn-sm" title="Edita">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                        }
                                        @if (item.besta_orco == true && item.bcompra_orco == 0)
                                        {
                                            <a href="@Url.Action("DeleteOrden_compra", new { id = item.ncode_orco })" class="btn btn-danger btn-circle btn-sm" title="Eliminar" onclick="return confirm('¿ Seguro de eliminar registro ?');">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        }

                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- end row -->